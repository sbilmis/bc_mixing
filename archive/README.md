# Archive

This folder is for local, non-deleted cleanup material.  It is intended for
generated, redundant, or superseded files that should not clutter the active
project root.

The current cleanup folder is:

```text
archive/2026-06-30_redundant_generated/
```

Its contents are ignored by git except for its README.  Nothing was deleted;
files were only moved here when they were clearly build artifacts, old
duplicates, or local downloads.
