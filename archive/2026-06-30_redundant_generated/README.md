# 2026-06-30 redundant/generated archive

This dated archive was created during the project-folder organization pass.

Moved here:

- `latex_build/`: LaTeX auxiliary/build outputs and `.DS_Store` files.
- `misc_downloads/`: local downloads such as `X-master.tar.gz`.
- `old_outputs/old/`: the pre-existing ignored `old/` folder with duplicate
  older scan and Monte Carlo outputs.

No scientific source files, active plotting scripts, final publication figures,
or D6 chunk files were moved here.

The contents are intentionally local/ignored so the git history stays focused
on source, notes, scripts, and selected numerical outputs.
